# 🪺 BudgetNest – Budgeting & Finance Helper

Simple Streamlit app to track expenses, visualize your budget, and get AI-based saving tips.

## Features

- Add expenses with amount, category, and optional description.
- Analyze your budget with total spend, category-wise breakdown, and overspending flag (threshold ₹500).
- Get AI-powered saving tips using Gemini + Tavily web search.
- Ask finance questions in natural language (text Q&A).
- NEW: Ask questions about uploaded **bill / bank statement images** using Gemini multimodal (text + image).
- Simple dashboard with expense table and bar chart by category.

## How to run

1. Create and activate a virtual environment (optional but recommended).
2. Install dependencies:
3. Create a `.env` file in the project root with:
4. Run the app: streamlit run app.py


